# Core
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
PyYAML>=6.0

# Distance matrix (step 1)
requests>=2.31

# Optimisation (step 3) — needs a Gurobi licence for large instances
gurobipy>=11.0

# Maps
folium>=0.15

# Notebooks (optional)
jupyter>=1.0
